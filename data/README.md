# Derived CS2 catalogs

Generated by `scripts/extract_items_game.py` from a local `data/raw/` dump of Valve `items_game.txt` + loc files.

- `data/raw/` is gitignored (Valve IP, ~22M). Extract commands: `docs/paths.md`.
- Commit these JSON/Markdown files, not the raw dump.
- Weapon catalogs: `ak47_paint_kits.json` (61), `glock_paint_kits.json` (59, includes Fade 38).
